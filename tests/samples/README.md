# JSONiq Sample Corpus

This folder contains small JSONiq files for manually testing the parser and language server.

- `valid/`: inputs that should parse without syntax diagnostics
- `invalid/`: inputs that should produce at least one syntax diagnostic
